---
applications:
- name: node-opa
  memory: 128M
  instances: 1
  buildpacks:
    - https://github.com/sap-staging/cloud-authorization-buildpack
    - nodejs_buildpack
  services:
    - ams
  health-check-type: process
  env:
    AMS_DATA:  >
            {
              "root": "/",
              "directories": ["cas/"]
            }
  routes:
  
