---
# This buildpack is DEPRECATED.
# It no longer ships an OPA sidecar. Only base DCL policy upload is performed.
# Migrate to the dedicated policies-deployer:
#   https://sap.github.io/cloud-identity-developer-guide/Authorization/DeployDCL.html
language: opa
include_files:
  - README.md
  - VERSION
  - bin/supply
  - manifest.yml
  - LICENSE
pre_package: scripts/build.sh
